const User = ({ name, location }) => {
  return (
    <div className="user-class">
      <h2>{name}</h2>
      <h3>{location}</h3>
      <h4>mob no. : 7392838298</h4>
    </div>
  );
};

export default User;
